# Examples 索引

本目录提供少量用于帮助 agent 理解分层边界的示例。

示例只用于解释架构，不代表项目必须实现对应模块。

## 强制规则

- MUST: 示例必须服务于分层理解。
- MUST: 示例中的模块名、协议名、外设名只作为边界说明。
- MUST NOT: 示例不得让某个具体模块成为项目默认核心。
- MUST NOT: 示例不得覆盖正式规范。

## 文件

- `layering-examples.md`: 常见错误分层和正确拆分方式。

